import { NotFoundComponent } from "@/components/ui/page-not-found";

const NotFound = () => {
  return (
    <div className="flex h-screen flex-1 items-center justify-center bg-background">
      <NotFoundComponent />
    </div>
  );
};

export default NotFound;
